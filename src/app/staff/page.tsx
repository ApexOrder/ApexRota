export default function StaffPage() {
  return (
    <div>
      <h1 style={{ fontSize: 22, fontWeight: 800, marginBottom: 10 }}>Staff</h1>
      <p style={{ opacity: 0.9 }}>
        Next step: create staff records (name, role, contract hours) and store them in a database.
      </p>
    </div>
  );
}
