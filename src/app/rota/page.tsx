export default function RotaPage() {
  return (
    <div>
      <h1 style={{ fontSize: 22, fontWeight: 800, marginBottom: 10 }}>Rota Builder</h1>
      <p style={{ opacity: 0.9 }}>
        Next step: calendar grid (staff rows × dates columns) with Full / Half AM / Half PM / Off.
      </p>
    </div>
  );
}
